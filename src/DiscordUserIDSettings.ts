/*
export class DiscordUserIDSettingsDialog extends FormApplication {
    protected _updateObject(event: Event, formData?: object): Promise<unknown> {
        throw new Error("Method not implemented.");
    }
    static init() {
        // iterate over all users in the game
        game.users.contents.forEach(user => {

        }
        // create a row consisting of a label and a text field
            // label has FoundryVTT User ID
            // text field is automatically populated with the corresponding user's Discord ID, or blank otherwise.
            // at the bottom, there's save settings and cancel button.
            // save settings commits changes.
    }
}
*/